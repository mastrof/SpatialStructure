export
    inertiatensor,
    asphericity,
    elongation,
    prolateness

δ(i, j) = i == j ? 1 : 0

function inertiatensor(config::T) where T <:AbstractVector{Particle{3,Float64}}
    I = [sum(for i in 1:3, j in 1:3]
end # function
